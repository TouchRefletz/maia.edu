/**
 * DEPRECATED — Replaced by CSS-only scroll hint (.has-h-overflow).
 * This file is kept empty to avoid import errors from cached builds.
 * Safe to delete once no references remain.
 */
export function initHorizontalChatScrollbar() {}
export function destroyHorizontalChatScrollbar() {}
